<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="card shadow">
      <div class="card-body p-5 text-center">
        {{#if success}}
          <i class="bi bi-check-circle display-1 text-success mb-4"></i>
          <h2 class="fw-bold text-success mb-3">¡Cuenta Activada!</h2>
          <p class="text-muted mb-4">
            Tu cuenta ha sido activada exitosamente. Ya puedes iniciar sesión y disfrutar de todos los servicios de AppCenar.
          </p>
          <a href="/auth/login" class="btn btn-success btn-lg">
            <i class="bi bi-box-arrow-in-right"></i> Iniciar Sesión
          </a>
        {{else}}
          <i class="bi bi-x-circle display-1 text-danger mb-4"></i>
          <h2 class="fw-bold text-danger mb-3">Error de Activación</h2>
          <p class="text-muted mb-4">
            {{#if error}}
              {{error}}
            {{else}}
              El enlace de activación es inválido o ha expirado. Por favor, contacta con soporte o solicita un nuevo enlace de activación.
            {{/if}}
          </p>
          <div class="d-grid gap-2">
            <a href="/auth/login" class="btn btn-outline-primary">
              <i class="bi bi-arrow-left"></i> Volver al Login
            </a>
          </div>
        {{/if}}
      </div>
    </div>
  </div>
</div>