<div class="container py-4">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow">
        <div class="card-body text-center p-5">
          <i class="bi bi-exclamation-triangle display-1 text-danger d-block mb-4"></i>
          
          <h3 class="fw-bold mb-3">¿Eliminar Dirección?</h3>
          
          <div class="alert alert-light text-start mb-4">
            <strong><i class="bi bi-geo-alt-fill text-danger"></i> {{direccion.nombre}}</strong>
            <p class="mb-0 text-muted mt-2">{{direccion.descripcion}}</p>
          </div>
          
          <p class="text-muted mb-4">
            Esta acción no se puede deshacer. La dirección será eliminada permanentemente.
          </p>

          <form action="/cliente/direcciones/eliminar/{{direccion._id}}" method="POST" class="d-grid gap-2">
            <button type="submit" class="btn btn-danger btn-lg">
              <i class="bi bi-trash"></i> Sí, Eliminar Dirección
            </button>
            <a href="/cliente/direcciones" class="btn btn-outline-secondary">
              <i class="bi bi-x-circle"></i> No, Cancelar
            </a>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>